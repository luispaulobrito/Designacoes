import { StatusBatismoIconesDirective } from './status-batismo-icones.directive';

describe('StatusBatismoIconesDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusBatismoIconesDirective();
    expect(directive).toBeTruthy();
  });
});
