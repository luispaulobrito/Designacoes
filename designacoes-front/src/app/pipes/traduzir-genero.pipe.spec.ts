import { TraduzirGeneroPipe } from './traduzir-genero.pipe';

describe('TraduzirGeneroPipe', () => {
  it('create an instance', () => {
    const pipe = new TraduzirGeneroPipe();
    expect(pipe).toBeTruthy();
  });
});
