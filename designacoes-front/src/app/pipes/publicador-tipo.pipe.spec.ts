import { PublicadorTipoPipe } from './publicador-tipo.pipe';

describe('PublicadorTipoPipe', () => {
  it('create an instance', () => {
    const pipe = new PublicadorTipoPipe();
    expect(pipe).toBeTruthy();
  });
});
